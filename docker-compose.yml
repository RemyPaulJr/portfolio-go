services:
  api:
    build: ./backend
    ports:
      - "8081:8080"
    depends_on:
      - mysql
    env_file:
      - .env

  mysql:
    image: mysql:8.4.8
    restart: always
    env_file:
      - .env
    ports:
      - "3306:3306"
    volumes:
      - mysql:/var/lib/mysql

volumes:
  mysql: